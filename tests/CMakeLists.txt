# Tests are discovered automatically via CTest.
# Add test executables here as the project grows.
#
# Example:
# add_executable(test_foo test_foo.cpp)
# target_include_directories(test_foo PRIVATE ${CMAKE_SOURCE_DIR}/include)
# add_test(NAME foo COMMAND test_foo)
